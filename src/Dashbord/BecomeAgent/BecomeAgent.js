import React from 'react';
import ApplyForAgent from '../../Pages/ApplyForAgent/ApplyForAgent';

const BecomeAgent = () => {
    return (
        <div>
            <ApplyForAgent></ApplyForAgent>
        </div>
    );
};

export default BecomeAgent;